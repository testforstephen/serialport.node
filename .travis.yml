language: node_js
node_js:
  - "6.9"

os:
  - linux

before_install:
  - npm install -g vsce
  - npm install -g gulp

install:
  - npm install

script:
  - gulp buildSerialPortLib

# safelist
branches:
  only:
  - master
